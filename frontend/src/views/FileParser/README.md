# FileParser

文件解析与初始化数据导入页面，对应路由 `/parser`。

## 页面职责

- 上传并解析 IDF/PCF 文件，展示解析预览与 Excel 下载入口。
- 上传初始化数据文件，并在用户确认后导入当前项目。
- 支持多个暂存解析结果合并、未确认结果离开提醒和当前项目初始化状态提示。

## 文件结构

- `index.vue`: 页面入口，维护上传、解析、合并、确认导入和离开守卫逻辑。
- `FileParserHeader.vue`: 页面标题区局部组件，封装当前页面对公共 `PageHeader` 的使用。

# PlanViewer

排产单查看与当日计划维护页面，对应路由 `/plans/:planKey`。

## 页面职责

- 按防腐、下料、焊接和甘特视图切换排产数据。
- 根据日期加载计划列表、文件列表、工作表和表格数据。
- 支持当日主计划文件编辑、完成标记、导入补丁、导出补丁和保存。

## 文件结构

- `index.vue`: 页面入口，维护路由同步、计划加载、文件编辑、日历和导入导出逻辑。
- `PlanViewerHeader.vue`: 页面标题区局部组件，封装当前页面对公共 `PageHeader` 的使用。

# SpoolCheck

管段校核页面，对应路由 `/spool-check`。

## 页面职责

- 加载当前项目管段、材料和焊口明细。
- 支持按装置过滤管段，切换结构、材料、焊口三个视图。
- 展示管段数量、材料数量、焊口数量和总长度等统计信息。

## 文件结构

- `index.vue`: 页面入口，维护管段数据加载、筛选、选择和统计派生数据。
- `SpoolCheckHeader.vue`: 页面标题区局部组件，封装当前页面对公共 `PageHeader` 的使用。

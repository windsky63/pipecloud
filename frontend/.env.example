# 浏览器请求的 API 根地址。开发环境保持相对路径，由 Vite 代理转发。
VITE_API_BASE_URL=/api/pipecloud

# 仅影响 `npm run dev`，不会写入生产业务逻辑。
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
VITE_PROXY_TIMEOUT_MS=1200000
